apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: investment-committee

resources:
  - namespace.yaml
  - configmap.yaml
  - secret.example.yaml   # rename to secret.yaml with real values before applying; never commit it
  - dependencies.yaml
  - observability.yaml
  - api.yaml
  - ingress.yaml
